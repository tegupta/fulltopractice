#########################Code to find lenght of string in C & python

#include<stdio.h>

void main()
{
        char str[100];
        int length,i;

        printf("Enter string ");
        gets(str);
        for(i=0;str[i]!='\0';i++)
        {
                length++;
        }

        printf("length of string is %s=%d",str,lenght);

}



#####################python code###############

string=input("Enter String")
count=0

for i in string:
        count=count+1

Print("length of string:")        
Print(count)

########code to repalce A,a with $ #############

def replace(st)
{
	for i in range(1, len(st)):
	if(st[i-1]=='A' or st[i-1]=='a' )
		st[i-1]=='$'

	for j in range(i, len(st)-1):
		st[j]=st[j+1]

	st[len(st)-]=' '
return
}


st=input(Enter String)
replace(st)

print(modified string:)
print(''.join(st))
